#Misc Variables
language: clojure
lein: 2.5.3
sudo: true

#Install Sass
before_install:
- brew install sassc

#Build
script: "lein uberjar && lein sassc once"

#Test
after_deploy: 'curl -X GET "https://api.ghostinspector.com/v1/suites/${CFD_GHOST_INSPECTOR_SUITE_ID}/execute/?apiKey=${CFD_GHOST_INSPECTOR_API_KEY}"'
