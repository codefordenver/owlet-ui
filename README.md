## OWLet UI | Code For Denver

### development 

`boot dev`

then visit: 
[http://localhost:3000](http://localhost:3000)


### clojurescript repl

`boot repl --cleint`

Then evaluate this at the repl:
`(start-repl)`