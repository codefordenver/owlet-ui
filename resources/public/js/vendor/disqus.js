var disqus_config  = function () {
                this.page.identifier = window.location.href.substr(window.location.href.lastIndexOf('/') + 1).substr(2);
                this.page.url = window.location.href;
                this.page.title = window.title;
              }
